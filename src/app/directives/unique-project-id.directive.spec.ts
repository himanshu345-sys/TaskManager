import { UniqueProjectIdDirective } from './unique-project-id.directive';

describe('UniqueProjectIdDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueProjectIdDirective();
    expect(directive).toBeTruthy();
  });
});
